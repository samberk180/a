export enum AppNavigationRoute {
  UnAuthentication = 'UnAuthenticated',
  Authenticated = 'Authenticated',
}

export enum AppUnAuthenticatedRoute {
  Login = 'Login',
  Register = 'Register',
  ResetPassword = 'ResetPassword',
  Verify = 'Verify',
}
